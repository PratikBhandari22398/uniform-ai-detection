<%- include('includes/boiler.ejs') %>
    <%- include('includes/nav.ejs') %>

        <style>
            :root {
                --bg: #000000;
                --accent: #E7FF00;
                --muted: #cfcfcf;
                --nav-height: 100px;
                /* SET THIS TO YOUR NAVBAR HEIGHT */
            }

            body {
                margin: 0;
                padding: 0;
                background: var(--bg);
                font-family: system-ui, sans-serif;
            }

            /* ------------ HERO SECTION (FULL SCREEN) ------------ */
            .hero {
                height: calc(100vh - var(--nav-height));
                width: 100%;
                background: url("/bg-pattern.png") center/cover no-repeat, #000;
                display: flex;
                align-items: center;
                justify-content: center;
                padding: 0 90px;
                box-sizing: border-box;
            }

            .hero-inner {
                max-width: 1500px;
                width: 100%;
                display: grid;
                grid-template-columns: 1.2fr 1fr;
                gap: 60px;
                align-items: center;
            }

            /* ---------------- LEFT CONTENT ---------------- */
            .hero-left {
                padding-left: 20px;
            }

            .eyebrow {
                color: var(--accent);
                font-weight: 700;
                letter-spacing: 1px;
                font-size: 13px;
                margin-bottom: 15px;
                text-transform: uppercase;
            }

            .hero-title {
                font-family: "DynaPuff", system-ui;
                font-size: 64px;
                line-height: 1.05;
                font-weight: 800;
                margin-bottom: 20px;
                color: white;
            }

            .hero-title span {
                color: var(--accent);
            }

            .hero-sub {
                color: var(--muted);
                max-width: 600px;
                font-size: 18px;
                line-height: 1.6;
                margin-bottom: 35px;
            }

            /* CTA BUTTONS */
            .btn-solid {
                background: var(--accent);
                padding: 12px 28px;
                color: #000;
                border-radius: 50px;
                font-size: 17px;
                font-weight: 800;
                text-decoration: none;
                display: inline-block;
                transition: 0.25s ease-in-out;
                box-shadow: 0 0 0 rgba(231, 255, 0, 0);
            }

            /* NEW HOVER EFFECT */
            .btn-solid:hover {
                transform: scale(1.07);
                background: #f0ff35;
                box-shadow: 0 0 20px rgba(231, 255, 0, 0.6),
                    0 0 40px rgba(231, 255, 0, 0.3);
            }

            /* ---------------- RIGHT SIDE ---------------- */
            .hero-right {
                position: relative;
                display: flex;
                justify-content: center;
                align-items: center;
            }

            /* Main hero image */
            .hero-image {
                width: 100%;
                max-width: 520px;
                height: 620px;
                border-radius: 26px;
                overflow: hidden;
                background: #111;
                box-shadow: 0px 40px 80px rgba(0, 0, 0, 0.6);
            }

            .hero-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

            /* Floating neon yellow cards */
            .hero-card {
                position: absolute;
                background: var(--accent);
                color: black;
                padding: 14px 20px;
                border-radius: 16px;
                font-weight: 700;
                font-size: 15px;
                box-shadow: 0 14px 40px rgba(231, 255, 0, 0.15);
                display: flex;
                align-items: center;
                gap: 12px;
            }

            .hero-card.small {
                top: 0;
                left: -50px;
            }

            .hero-card.badge {
                bottom: 0;
                right: -50px;
            }

            /* Responsive */
            @media (max-width: 950px) {
                .hero {
                    padding: 20px;
                    height: auto;
                }

                .hero-inner {
                    grid-template-columns: 1fr;
                    text-align: center;
                }

                .hero-left {
                    padding-left: 0;
                }

                .hero-title {
                    font-size: 40px;
                }

                .hero-image {
                    max-width: 360px;
                    height: 450px;
                    margin: 0 auto;
                }

                .hero-card.small,
                .hero-card.badge {
                    position: static;
                    margin: 10px auto;
                }
            }

            /* ======= AUTO-REVERSE TICKER BAR ======= */

            /* Outer bar */
            .service-bar {
                width: 100%;
                background: #E7FF00;
                overflow: hidden;
                padding: 16px 0;
                border-top: 1px solid #cddf00;
                border-bottom: 1px solid #cddf00;
                white-space: nowrap;
            }

            /* The moving track */
            .service-track {
                display: inline-flex;
                gap: 60px;
                /* space between each item */
                animation: marquee 10s linear infinite;
            }

            /* Items */
            .service-track span {
                font-size: 22px;
                font-weight: 600;
                color: #000;
                display: inline-block;
            }

            /* KEYFRAME — smooth, endless scrolling */
            @keyframes marquee {
                0% {
                    transform: translateX(0);
                }

                100% {
                    transform: translateX(-50%);
                }
            }
        </style>

        <!-- ---------------- HERO SECTION ---------------- -->
        <section class="hero">
            <div class="hero-inner">

                <!-- LEFT TEXT -->
                <div class="hero-left">

                    <div class="eyebrow">Hey, welcome to UniformCheck Pro</div>

                    <h1 class="hero-title">
                        Smart Monitoring,<br>
                        Real Impact —
                        <span>Powered by AI</span>
                    </h1>

                    <p class="hero-sub">
                        Student Monitor is an innovative, AI-driven platform designed to help educational institutions
                        automate
                        uniform and ID-card verification with accuracy and speed. Powered by advanced computer vision
                        and deep
                        learning.
                    </p>

                    <a href="#" class="btn-solid">Get Started</a>

                </div>

                <!-- RIGHT IMAGE + FLOATING LABELS -->
                <div class="hero-right">

                    <!-- Top floating badge -->
                    <div class="hero-card small">
                        <span style="background:#000;color:#fff;padding:6px 12px;border-radius:8px;">200k+</span>
                        <span>Clients</span>
                    </div>

                    <!-- Main image -->
                    <div class="hero-image">
                        <img src="/home-1.png" alt="hero">
                    </div>

                    <!-- Bottom floating badge -->
                    <div class="hero-card badge">
                        <div style="background:#000;color:#fff;padding:10px;border-radius:10px;">▶</div>
                        <span>Founded in 2025</span>
                    </div>

                </div>

            </div>

        </section>
        <div class="service-bar">
            <div class="service-track">
                <span>✦ AI Uniform Detection</span>
                <span>✦ ID Card Verification</span>
                <span>✦ Real-Time Alerts</span>
                <span>✦ Automated Reporting</span>

                <!-- duplicated for seamless looping -->
                <span>✦ AI Uniform Detection</span>
                <span>✦ ID Card Verification</span>
                <span>✦ Real-Time Alerts</span>
                <span>✦ Automated Reporting</span>
            </div>
        </div>
        <!-- Insert about.ejs here -->
        <%- include("about") %>
            <%- include("workflow") %>
                <%- include("benefits") %>
                    <%- include("team") %>

                        <%- include('includes/footer.ejs') %>